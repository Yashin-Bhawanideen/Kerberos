rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Deny all direct client access.
      // Only the API's Firebase Admin SDK (using the service account) bypasses these rules.
      allow read, write: if false;
    }
  }
}
